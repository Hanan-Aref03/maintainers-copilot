# ENH: Add a `df.corrupt()` accessor for synthetic dirty-data generation

You are triaging a pandas feature request.

Issue summary:
- The reporter wants a DataFrame accessor that can inject controlled faults, typos, missing values, and outliers.
- The use case is data-science education and testing workflows that need realistic dirty data.
- The request is for a convenient built-in way to generate intentionally broken data without writing a custom helper every time.

Task:
- classify the issue
- summarize the user need
- note whether the request sounds like a core API candidate or a downstream utility
- write a short maintainer reply

Return:
- label
- rationale
- next step
- maintainer reply

